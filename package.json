{
  "name": "pi-carryover",
  "version": "1.1.0",
  "description": "Cross-session work carryover for pi: auto-loaded project state notes + /resume session linkage, with 3-tier reliability (agent-maintained notes, LLM exit summary, raw fallback). v1.1: topic-aware compaction — detects topic shifts and suggests/auto-compacts old context, archiving every compaction summary as a topic archive.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package"
  ],
  "files": [
    "extensions",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test test/"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "author": "Feng-H",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Feng-H/pi-carryover.git"
  },
  "bugs": {
    "url": "https://github.com/Feng-H/pi-carryover/issues"
  },
  "homepage": "https://github.com/Feng-H/pi-carryover#readme",
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.85.1",
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "typescript": "^7.0.2"
  }
}
